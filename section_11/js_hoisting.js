// refrence error = if variable nt defined/declared

// but if it is declared and not initialized then it will give undefined

console.log(a); //undefined because only declarations are hoisted not initialization
var a = 2; 
